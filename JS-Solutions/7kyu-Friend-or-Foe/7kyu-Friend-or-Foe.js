function friend(friends){
    return friends.filter((x) => x.length === 4)
    //your code here
  }

  //create empty array
  //loop through friends
  //if current index of frieds has a length of 4, push the value to the new array
  //return the new array

  //
