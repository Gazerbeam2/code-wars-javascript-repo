function getChar(c){
    let numberconversion = parseInt(c, 10);

    return String.fromCharCode(numberconversion);
}

//convert c into a number
//find the unicode of that number
//turn it back into a string
