//Solutions

function between(a, b){
    let newArray = []

    for(let i = a; i <=b; i++){
        newArray.push(i);
    }return newArray
}
//Given two numbers
//Are the numbers all positive? Are negative numbers included?
//Are Strings a possibility
//Create new Array
//Start For Loop at first Parameter
//Set the ending condition less than or equal to the second Parameter
//Push the current index value to the new Array variable
//Return the value of the new array
