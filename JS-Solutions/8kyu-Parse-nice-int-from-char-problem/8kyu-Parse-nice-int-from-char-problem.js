// Solution

function getAge(inputString){
    return parseInt(inputString[0])
    }

//Find the first index of the string since it will always be a number
//Use parseInt to convert it into a number and return that value
