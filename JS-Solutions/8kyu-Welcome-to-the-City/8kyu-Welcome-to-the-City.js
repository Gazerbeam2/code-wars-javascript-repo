function sayHello( name, city, state ) {
    return `Hello, ${name.join(' ')}! Welcome to ${city}, ${state}!`
  }



  //The primary goal here is to take thise inputs and return them into a string
  //Name will be in a array and will needed to be joined into a string
  //join the name input and interpolate the rest of the inputs into a string.
